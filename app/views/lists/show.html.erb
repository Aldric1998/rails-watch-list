<h1>List: </h1>
<p>check al those movies out : </p>

<ul class="list-group">
  <h2><%= @list.name %></h2>
  <% @list.bookmarks.each do |bookmark| %>
    <li class="list-group-item"><%=bookmark.movie.title %>
    <p><%= bookmark.movie.overview %></p>
    <img id="first_image" src ="<%= bookmark.movie.poster_url %>">
    <p><%= bookmark.movie.rating %></p></li>
    <p><%= bookmark.comment %></p></li>
  <%end%>
</ul>
<p> ----- <%= link_to 'bookmarking', new_list_bookmark_path(@list)%> ---- </p>
<p> ----------- </p>
<p> ----- <%= link_to 'back', lists_path%> ---- </p>
